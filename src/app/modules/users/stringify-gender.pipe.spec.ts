import { StringifyGenderPipe } from './stringify-gender.pipe';

describe('StringifyGenderPipe', () => {
  it('create an instance', () => {
    const pipe = new StringifyGenderPipe();
    expect(pipe).toBeTruthy();
  });
});
